# Smart Market Watchlist — backend dependencies
#
# Everything below installs from wheels on CPython 3.11 - 3.14 (verified on 3.14).
# There is deliberately no pandas / scipy: the statistics we need (OLS beta,
# rolling correlation, z-scores, BOCPD) are a few dozen lines each and reading
# them is part of the point. numpy alone carries the vector maths.

fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0.30
pydantic>=2.9
pydantic-settings>=2.4
aiosqlite>=0.20          # zero-setup DB fallback — no Postgres required
httpx>=0.27              # yahoo adapter + openrouter provider
numpy>=1.26

# ---------------------------------------------------------------------------
# Optional. The app runs fully without every line below this comment.
# ---------------------------------------------------------------------------
asyncpg>=0.29            # only used when DATABASE_URL points at Postgres
redis>=5.0               # only used when REDIS_URL is reachable; else dict shim
google-genai>=0.3        # only used when GEMINI_API_KEY is set

# dev
pytest>=8.0
pytest-asyncio>=0.23
